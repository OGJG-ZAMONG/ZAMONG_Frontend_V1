import { sellDetail } from "../../../models/dto/response/sellDreamDetailResponse";
import * as S from "./styles";

interface PropsType {
  postData: sellDetail;
}

const Posting = ({postData} : PropsType) => {
  return (
    <div>

    </div>
  )
};

export default Posting;
