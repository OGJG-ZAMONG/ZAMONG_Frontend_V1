import { FC } from "react";
import * as S from "./styles";

interface Props {
    title: string
}

const DiaryTitle: FC<Props> = ({title}):JSX.Element => {
    return (
        <S.Container>
            
        </S.Container>
    );
}

export default DiaryTitle;