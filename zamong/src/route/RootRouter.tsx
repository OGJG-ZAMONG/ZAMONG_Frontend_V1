import { BrowserRouter, Route, Switch } from "react-router-dom";

const RootRouter = () => {
    return (
        <BrowserRouter>
            <Switch>

            </Switch>
        </BrowserRouter>
    )
}

export default RootRouter;